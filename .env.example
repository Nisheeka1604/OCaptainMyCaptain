# API (apps/api)
PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/ocaptain
JWT_SECRET=your-secret-key-change-in-production

# Web (apps/web) - optional if calling api from same host in dev
NEXT_PUBLIC_API_URL=http://localhost:4000
